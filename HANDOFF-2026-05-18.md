# Handoff — skill-tree as of 2026-05-18 evening

For Daniel picking up after reboot, or any agent walking in cold. Reading time: ~5 minutes.

## The big shift today

skill-tree was reframed from "a skill router for one Mac" into the **institutional-knowledge layer for the AI-skill ecosystem at large**. The pivot landed mid-session after dogfood validation showed the original ADR (per-project provisioning as primary) was made on bad data.

The product is now: **documented expertise about every agent platform, every skill packager, every installer, every common failure mode — consumed by the AI inside the host agent to navigate the ecosystem on the user's behalf.**

Read in this order for context:
1. `docs/adr/0003-institutional-knowledge.md` — the strategic decision
2. `docs/ecosystem-map.md` — the global ecosystem (refreshed 2026-05-18 with 12 new entries)
3. `docs/registry-map.md` — per-machine layout
4. `docs/validation-2026-05-18.md` — the data behind the reframe

## Today's work, in commit order

| Issue | What | State |
|---|---|---|
| BRO-179 | `/skill-tree:check` cleanup, 5 errors → 0 | ✅ merged |
| BRO-180 | Reach@catalog metric + corpus + simulation scripts | ✅ merged |
| BRO-181 | ADR 0001 (per-project pivot) — **superseded same day** | Closed |
| BRO-182 | `/skill-tree:provision` per-project skill provisioner | ✅ merged |
| BRO-183 | `.codex-plugin/` manifest + marketplace registration | ✅ merged (with post-merge layout fix in PR #10) |
| BRO-184 | Index plugin-namespaced skills in catalog — **the load-bearing fix** | ✅ merged |
| BRO-185 | ADR 0002 (hybrid global-base + per-project tail) | ✅ accepted, merged |
| BRO-186 | `bin/skill-tree` cross-platform dispatcher | ✅ merged |
| BRO-187 | `/skill-tree:sync` for project manifest drift | ✅ merged |
| BRO-188 | `archive` command for unused personal skills | ✅ merged |
| BRO-189 | Scanner gap: `~/.agents/`, project `.claude/skills/` | Backlog — not blocking |
| BRO-190 | Umbrella: institutional-knowledge layer (ADR 0003) | ✅ merged |
| **BRO-191** | **`/skill-tree:catalog <topic>`** | **🚧 in progress — feature branch `daniel/catalog-skill-bro-191`** |
| BRO-192 | `/skill-tree:diagnose <problem>` | Backlog |
| BRO-193 | `/skill-tree:install <ref>` cross-installer dispatcher | Backlog |
| BRO-194 | Process for keeping ecosystem-map.md current monthly | Backlog |

12 PRs opened and merged today (PR #8 through PR #12). All on `main`. Branch protection is on main; never push direct except for documented hotfixes.

## Where you are RIGHT NOW

You're on branch `daniel/catalog-skill-bro-191` with one staged file:
- `skills/catalog/SKILL.md` — the new catalog skill, written and ready, **not yet committed**

To pick up the catalog work:

```bash
cd ~/Projects/skill-tree
git status   # confirm you're on daniel/catalog-skill-bro-191 with catalog/SKILL.md staged
git add skills/catalog/SKILL.md
git commit -m "feat(catalog): /skill-tree:catalog <topic> recommends from ecosystem-map (BRO-191)"
git push -u origin daniel/catalog-skill-bro-191
gh pr create --title "feat(catalog): /skill-tree:catalog <topic> (BRO-191)" --body "..."
```

After the PR merges, **reload Claude Code plugins** (`/reload-plugins` slash command works) and the catalog skill appears in the available-skills list. Then dogfood it the same way audit was dogfooded (invoke via Skill tool, confirm it reads the docs, recommends from them).

## State of your local machine

This session also did real cleanup against your actual setup:

- `~/.openclaw/` — removed (was a March dev artifact)
- 15 unused personal skills archived to `~/.claude/skills-archive-2026-05-18-1755/`
- 5 stale plugin-cache version dirs removed (~28 MB)
- `~/.agents/.skill-lock.json` — `lastSelectedAgents` trimmed from 13 → 4 (kept claude-code, codex, gemini-cli, antigravity), one stale entry (`axiom-swiftdata`) removed
- skill-tree installed into Codex (`~/.codex/config.toml` has `[plugins."skill-tree@danielbrodie"]`)
- last30days fully provisioned: 17 env vars set including the new SERPER_API_KEY, GITHUB_TOKEN, OPENROUTER_API_KEY, CT0
- `digg-pp-cli` installed at `~/go/bin/digg-pp-cli` — Digg becomes an auto-source on next last30days run

## High-leverage next moves (after the catalog PR merges)

In rough priority order:

1. **Dogfood `/skill-tree:catalog`** — fresh Claude Code session, type "find me a skill for X", verify the model reads `ecosystem-map.md` and recommends from it. Iterate the SKILL.md if it doesn't.
2. **BRO-192 `/skill-tree:diagnose`** — the third institutional-knowledge skill. Walks failure-mode signatures to debug specific issues.
3. **BRO-193 `/skill-tree:install`** — cross-installer dispatcher. Picks the right CLI per ref type.
4. **Publish `ecosystem-map.md` externally.** HN / X / r/ClaudeAI post linking the doc invites community PRs. The doc IS the product per ADR 0003; outside contribution is how it stays current at scale.
5. **BRO-194** — recurring process for keeping `ecosystem-map.md` current (monthly survey + PR).
6. **Apply the institutional-knowledge pattern to MCP servers and hooks.** Same docs+audit+catalog+diagnose+install pattern can cover MCP setups and Claude Code hooks. Substantial product expansion.

## Things that look broken but aren't

- **`~/.agents/` directory still exists.** That's intentional — it's the upstream source-of-truth for skills installed via `npx skills` (the vercel-labs/skills installer). The symlinks in `~/.claude/skills/` point to it. Don't delete.
- **`docs/adr/0001-routing-vs-provisioning.md` is "Accepted" briefly then "Superseded".** That's an accurate record of how the decision evolved within one day. Don't clean it up — same-day reversal is part of the story.
- **Two `superpowers` plugins were installed.** I removed `superpowers@superpowers-marketplace` (stale v4.3.1) and kept `superpowers@claude-plugins-official` (v5.1.0). If `npx skills sync` ever runs, it may try to recreate the duplicate — keep an eye on `installed_plugins.json`.

## Things to verify on reboot

After Claude Code restarts:

```bash
# 1. Are the new skills loaded?
~/Projects/skill-tree/bin/skill-tree --help | grep -E "audit|sync|provision|catalog"

# 2. Did the last30days status banner pick up Digg?
# Should say "Ready — 11 sources active" (was 10)
# Look at SessionStart hook output in the new session

# 3. Is the merge state clean?
cd ~/Projects/skill-tree && git log --oneline -10

# 4. Are catalog SKILL.md available?
ls ~/.claude/plugins/cache/skill-tree/skill-tree/0.6.0/skills/
```

## Open question for you to decide

**ADR 0003's "publish ecosystem-map.md externally" decision-trigger.** The doc is now genuinely useful enough to publish for outside contribution. But doing so exposes skill-tree to public attention and starts a maintenance commitment to respond to PRs. Worth deciding before the catalog PR lands whether to time a public launch around it.

## Contacts in your own head

- **zkiihne** — pushed back on per-project provisioning at 3:27 PM. Their critique drove the hybrid in ADR 0002. Worth a re-share once catalog ships — they'd recognize themselves in the design history.
- **mvanhorn** — maintains `last30days-skill` and `printing-press-library`. You also use his `digg-pp-cli` now. He'd be interested in skill-tree as a downstream consumer story.
- **Matt Pocock** — his `setup-matt-pocock-skills` pattern is part of the foundation. His skills repo is referenced in ecosystem-map.md.

---

You're set. Read the ADR chain in `docs/adr/` and `docs/ecosystem-map.md`, run `git log --oneline -15` to see today's commits, and pick up at the catalog skill commit on the feature branch.
