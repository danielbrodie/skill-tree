// skill-tree OpenClaw plugin — skills-only, no TypeScript logic needed.
// All functionality is delivered via plugin-shipped skills that invoke Python scripts.

export default function register() {
  // Skills handle everything.
}
