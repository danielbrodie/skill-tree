{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "uv run ${CLAUDE_PLUGIN_ROOT}/scripts/check.py --quiet --notify 2>&1 || true"
          }
        ]
      }
    ]
  }
}
